{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the UsageBar settings window.",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
